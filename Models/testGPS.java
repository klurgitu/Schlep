/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package Models;

import javafx.application.Application;
import javafx.stage.Stage;

/**
 *
 * @author josia
 */
public class testGPS extends Application {

    @Override
    public void start(Stage stage) throws Exception {
        //URL urlForDropdownFX = new File("/src/view/GPSView.fxml").toURI().toURL();
        //Parent parent = FXMLLoader.load(urlForDropdownFX);
        //Scene scene = new Scene(parent);
        /**
         * 
         *  URL mediaUrl = getClass().getResource("GPS_Stub.mp4");
        String mediaStringUrl = mediaUrl.toExternalForm();
        Media media = new Media (mediaStringUrl);
        final MediaPlayer player = new MediaPlayer(media);
        MediaView gpsMedia = new MediaView(player);
        //MediaPlayer player = new MediaPlayer(video);
        player.setAutoPlay(true);
        player.play();
        *  stage.setTitle("Schlep");
        //stage.setScene(scene);
        stage.show();
    }

    public static void main(String[] args) {
        launch(args);
    }
         * 
         */
       
        //MediaView tracking = new MediaView(player);
         

       
    }
  



}
